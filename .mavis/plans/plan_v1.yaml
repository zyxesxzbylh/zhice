version: 1
plan:
  name: "司辰 (FlowSync) 全面优化"
  max_concurrency: 6
  max_consecutive_failures: 2
  max_cycles: 12
  auto_reject_retries: 1
  verifier_config:
    default_verifiers: [verifier]
    audit_sample_rate: 0.0
tasks:
  - id: foundation-contract
    title: "foundation"
    assigned_to: coder
    max_retries: 1
    prompt: "Build the shared foundation: theme tokens, task helpers, keyboard hooks, ui primitives. Working dir: D:\工作管理\flowsync. Don't install deps. Output deliverables.md listing files."
    verified_by: verifier
    verify_prompt: "Run npx tsc --noEmit and npm run build. Both must pass. Check files exist."